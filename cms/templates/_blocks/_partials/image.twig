{% set image = image ?? null %}
{% if not image and block.images.one() %}
    {% set image = block.images.one() %}
{% endif %}
{% if image %}
    <img src="{{image.url}}" alt="{{image.title}}" />
{% endif %}